[User]
username = root
password =


[Connect]
server = 127.0.0.1
port = 3306
db= fang_cti

[Command]
command = (SELECT * FROM thehackernews) 
        UNION 
        (SELECT * FROM bleepingcomputer)
        UNION 
        (SELECT * FROM nakedsecurity)
        UNION 
        (SELECT * FROM securelist)
        UNION 
        (SELECT * FROM securityaffairs)
        UNION 
        (SELECT * FROM welivesecurity)
        UNION 
        (SELECT * FROM hackread)

db_table = fang's database

